<!-- 
GoldFlow Pro Favicon

To add a favicon to your application:

1. Create or obtain a favicon image (16x16, 32x32, or 64x64 pixels)
   - Can be .ico, .png, or .svg format
   - Recommended: Create one with the diamond/gold symbol

2. Place the favicon file at: src/favicon.ico
   (Or update the path in src/index.html if using a different name)

3. The favicon reference is already in src/index.html:
   <link rel="icon" type="image/x-icon" href="favicon.ico">

4. Examples of favicon generation:
   - Online tools: https://favicon-generator.org
   - Use a diamond or gold emoji
   - Use the GoldFlow Pro logo

5. Build the application to include the favicon:
   npm run build:prod

The favicon should appear in the browser tab when running the application.
-->
